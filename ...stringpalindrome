bool isPalindrome(string s) {
        string snew = "";
        for(int i=0;i<s.size();i++){
            if(isalnum(s[i])) snew+=tolower(s[i]);
        }
        for(int i=0;i<snew.size()/2;i++){
            if(snew[i]!=snew[snew.size()-i-1]) return false;
        }
        return true;
    }
